//定数ageが定義されています。初期値は数値の33です。
const age = 40;
// 1.
//ageの値が30以上かつ、40未満という条件のif文を作成します。
if(age >= 30 && age < 40 ){
    console.log("私の年齢は３０代です。");
}

// 2.
//ageの値が29以下または、40以上という条件のif文を作成します。
if(age <= 29 || age >= 40){
    console.log("私の年齢は３０代ではありません。");
}
